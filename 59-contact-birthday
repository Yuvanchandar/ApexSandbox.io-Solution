public Boolean isBirthday(Contact c) {
    //code here
    if(c1.Birthdate != null){
        Date today = System.today();
        if(c1.Birthdate.month() == today.month() && c1.Birthdate.day() == today.day()){
                return true;
        }
        return false;
    }
}